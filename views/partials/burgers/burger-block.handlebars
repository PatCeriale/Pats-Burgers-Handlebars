<li>
	{{name}}
	<br>
	<br>
	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR!
		{{else}}UNDEVOUR! (gross){{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
{{!-- <li>
	<div>
		{{#if devoured}}
		<div class="burgerdiv">{{name}}</div>
		<button class="change-devoured" data-id="{{id}}" data-devoured="{{devoured}}">Devour it!</button>
		{{else}}
		<div class="eaten-burger">{{name}}</div>
		<button class="delete-burger" data-id="{{id}}">Delete</button>
		{{/if}}
	</div>
</li> --}}